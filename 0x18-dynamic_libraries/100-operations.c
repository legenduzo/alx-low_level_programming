/**
 * add - adds 2 bumbers and returns the result
 * @a: first number
 * @b: second number
 *
 * Return: int
 */

int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - subtracts 2 bumbers and returns the result
 * @a: first number
 * @b: second number
 *
 * Return: int
 */

int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - multiplies 2 bumbers and returns the result
 * @a: first number
 * @b: second number
 *
 * Return: int
 */

int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - divides 2 bumbers and returns the result
 * @a: first number
 * @b: second number
 *
 * Return: int
 */

int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - mods 2 bumbers and returns the result
 * @a: first number
 * @b: second number
 *
 * Return: int
 */

int mod(int a, int b)
{
	return (a % b);
}

#include <stdio.h>

/* A printf function */

/**
 * main -  The entry point of the program
 * Description: Prints something
 *
 * Return: Returns with an 0 (Always success)
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");

	return (0);
}

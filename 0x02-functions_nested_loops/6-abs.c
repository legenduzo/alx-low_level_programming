/* This function returns absolute values */

/**
 * _abs - returns absolute numbers
 * @n: number to return absolute of
 *
 * Return: integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
